{
  "name": "lumi-macro-lab",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "Ephemeral macro resolution preview for Lumiverse.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
